import React from 'react';
import "../styles/TodoList.css";

const TodoList = () => {
    return (
        <div id="todo-container">
            <p>This is the container that encapsulates the to-do items.</p>
        </div>
    );
}

export default TodoList;